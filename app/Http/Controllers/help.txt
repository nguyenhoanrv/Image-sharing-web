mình có table comments(id, post_id, user_id, content, par_id) par_id là id của comment bố

mình muốn dữ liệu có dạng :

comments = [
  [id, user_id, content,..., [comment_child]],
  [id, user_id, content,..., [comment_child]],
  [id, user_id, content,..., [comment_child]],
]
đầu tiên sẽ lấy ra tất cả comment có post_id và par_id = -1  (par_id là -1 thì sẽ không phải là con của comment nào)

sau đó dùng foreach mảng vừa lấy được để lấy từng comment có par_id = id comment hiện tại và post_id =post_id xong 
push vào comment_child